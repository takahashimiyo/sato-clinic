# サイト制作ルールブック

> Webサイト制作時の「デザイン・コーディング・原稿・画像・修正・納品」の共通ルールをまとめたドキュメントです。  
> 案件固有の指示がある場合は、**案件固有の指示を優先**します。

---

## 1. このルールブックの目的

このルールブックは、以下を防ぐことを目的とします。

- デザインカンプと実装のズレ
- 原稿の勝手な変更・追加
- 画像の差し替えミス、パス切れ
- 修正時に関係ない箇所まで変更してしまうこと
- 制作者によるコーディングルールのばらつき
- 納品後にHTMLを開いた際、画像やCSSが表示されない問題
- 後から修正しにくいHTML / CSSになること

---

## 2. 指示の優先順位

内容に矛盾がある場合は、原則として以下の順で判断します。

1. **最新の確定指示・修正指示**
2. **制作依頼書・仕様書**
3. **掲載原稿・基本情報マスタ**
4. **完成デザイン・デザインカンプ**
5. **本ルールブック**
6. 一般的なWeb制作の慣習

### 重要

- 後から出された明確な修正指示は、過去の指示より優先する。
- 判断できない場合は、勝手に補完せず確認する。
- 原稿や数値について複数の情報がある場合は、指定された「正」の資料を優先する。

---

## 3. 基本方針

### MUST

- デザインカンプをできる限り忠実に再現する。
- 指定されていない文章を勝手に追加しない。
- 指定されていない画像へ勝手に差し替えない。
- HTML / CSS / 画像の参照は、納品フォルダ内で完結させる。
- ファイルパスは原則として相対パスを使用する。
- 修正依頼では、指定された箇所以外を不用意に変更しない。

### SHOULD

- HTMLは意味のある構造にする。
- CSSは再利用しやすく、後から修正しやすい形にする。
- 高齢者を含む幅広いユーザーが読みやすい文字サイズ・行間を意識する。
- デザイン上の見た目だけでなく、情報の優先順位や導線も考慮する。

---

# 4. ファイル構成

基本構成は以下とします。

```text
project/
├─ index.html
├─ css/
│  └─ style.css
└─ assets/
   ├─ images/
   ├─ icons/
   └─ logo/
```

案件によって画像数が少ない場合は、以下でも可とします。

```text
project/
├─ index.html
├─ css/
│  └─ style.css
└─ assets/
   ├─ logo.png
   ├─ main.jpg
   └─ staff.jpg
```

## 禁止事項

- `C:\Users\...` などのローカル絶対パス
- 制作者PCにしか存在しないファイルへの参照
- 納品ZIPの外にある画像への参照
- 不要な一時ファイル・バックアップファイルの混入

---

# 5. HTMLルール

## 5-1. 基本設定

```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>ページタイトル</title>
  <link rel="stylesheet" href="./css/style.css">
</head>
```

## 5-2. セマンティックHTML

適切な要素を使用します。

- `header`
- `nav`
- `main`
- `section`
- `article`
- `footer`
- `h1`〜`h6`
- `ul` / `ol`
- `table`
- `address`

### 原則

- レイアウトだけを目的に、すべてを `div` にしない。
- ページ内の `h1` は原則1つ。
- 見出しレベルを飛ばさない。
- ボタンのように見えてページ遷移するものは、原則 `a` を使用する。
- 本当に操作を行うものは `button` を使用する。

---

# 6. CSSルール

## 6-1. 共通設定

最低限、以下を設定します。

```css
*,
*::before,
*::after {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
}

img {
  display: block;
  max-width: 100%;
}
```

---

## 6-2. CSSの記述場所

原則として、CSSは以下にまとめます。

```text
css/style.css
```

### 原則禁止

- HTML内への大量のインラインCSS
- 同じ指定の重複
- 理由のない `!important` の多用

---

## 6-3. CSS変数

サイト全体で繰り返し使用する色・幅などは、可能な限り変数化します。

```css
:root {
  --color-text: #171717;
  --color-primary: #0f924a;
  --color-bg: #f4f9fa;
  --color-white: #ffffff;

  --content-width: 1200px;
}
```

---

## 6-4. クラス名

意味が分かる名前を使用します。

### 良い例

```css
.site-header
.global-nav
.hero
.news-list
.section-title
.staff-card
.access-map
```

### 避ける例

```css
.box1
.box2
.left1
.green-text2
.aaa
```

---

# 7. レイアウトルール

## 7-1. デザインカンプ基準

- デザインカンプのフレーム幅を基準に実装する。
- 横幅だけでなく、以下も確認する。

  - 上下余白
  - 左右余白
  - セクション間隔
  - テキストの折り返し位置
  - 写真の表示範囲
  - 見出しと本文の距離
  - ボタンの位置
  - 線・角丸・影

---

## 7-2. コンテンツ幅

サイト全体をフル幅にせず、主要コンテンツには必要に応じて最大幅を設定します。

```css
.inner {
  width: min(1200px, calc(100% - 80px));
  margin-inline: auto;
}
```

※実際の数値はデザインカンプを優先します。

---

## 7-3. 余白

余白はその場しのぎで決めず、可能な範囲で統一します。

例：

```text
小      8px / 12px / 16px
中      24px / 32px / 40px
大      56px / 64px / 80px
特大    100px / 120px
```

デザイン再現が優先されるため、必要に応じて個別値を使用して構いません。

---

# 8. 文字・フォント

## 基本

- 日本語フォントは案件指定を優先する。
- 指定がない場合は、可読性が高くWebで利用しやすいフォントを選ぶ。
- 本文・見出し・ボタンで文字の太さにメリハリをつける。

### 推奨例

```css
font-family:
  "Noto Sans JP",
  "Hiragino Kaku Gothic ProN",
  "Yu Gothic",
  sans-serif;
```

### ウェイト例

```text
Regular  400
Medium   500
Bold     700
```

## 可読性

- 本文は小さくしすぎない。
- 行間は本文サイズの約1.6〜2.0倍を目安とする。
- 高齢者向けサイトでは特に小さい文字を避ける。
- リンクやボタンは本文と見分けやすくする。

---

# 9. 色

## 原則

- デザインカンプ・ブランドカラーを優先する。
- 似た色を必要以上に増やさない。
- 本文と背景のコントラストを確保する。
- 装飾色よりも、情報の読みやすさを優先する。

## CSS例

```css
:root {
  --color-text: #171717;
  --color-primary: #0f924a;
  --color-bg: #f4f9fa;
  --color-white: #ffffff;
}
```

---

# 10. 画像ルール

## 10-1. 使用画像

- 原則として支給画像を使用する。
- 支給外画像を勝手に追加しない。
- 生成AI画像を使用する場合も、事前に使用可否を確認する。
- 元画像そのものを不用意に加工・上書きしない。

## 10-2. パス

```html
<img src="./assets/doctor.jpg" alt="院長">
```

### NG

```html
<img src="C:\Users\name\Desktop\doctor.jpg">
```

---

## 10-3. トリミング

人物画像では、特に以下に注意します。

- 顔が切れない
- 頭頂部が切れない
- 左右の肩や身体が不自然に切れない
- 人物が中央に見えるとは限らないため、`object-position` で調整する

```css
.staff-photo img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: 45% 30%;
}
```

### 注意

「画像を左へ100px移動」といった指示がある場合、単にコンテナを移動するのではなく、

- `object-position`
- `transform`
- 画像幅
- コンテナの `overflow`

のどれを変更すべきか確認した上で実装します。

---

# 11. アイコン

- サイト全体のテイストを統一する。
- 同じ種類のリンクには、原則として同じアイコンを使用する。
- アイコンだけで意味を伝えない。
- 可能であれば文字ラベルを併記する。

---

# 12. ボタン・リンク

## 原則

- 同じ役割のボタンはデザインを統一する。
- hover時にも文字が読めること。
- ボタンサイズ・角丸・影・アイコン位置を統一する。
- 遷移先が分かる文言にする。

### 例

```text
診療内容について
診療方針・理念について
よくあるご質問
お知らせ一覧
```

---

# 13. 原稿・テキストルール

## 最重要

確定原稿が支給されている場合は、**原則として文言を改変しません**。

### 禁止

- AIによる勝手な要約
- 語尾の書き換え
- 数字の変更
- 固有名詞の修正
- 「分かりやすくするため」の文章追加
- ダミー情報を実在情報へ勝手に変更

## 文章変更が必要な場合

以下のいずれかがある場合のみ変更します。

- クライアントから明示的な変更指示がある
- 最新版原稿が支給された
- 誤字修正について承認された

---

# 14. 情報の正本

院名・住所・電話番号・診療時間など、複数ページで繰り返し使用する情報は、必ず「基本情報マスタ」などの正本を基準にします。

### 対象例

- 施設名
- 担当者名
- 電話番号
- 住所
- メール
- 営業時間 / 診療時間
- 休業日 / 休診日
- 最寄駅
- 駐車場

複数箇所に記載する場合は、すべて同じ内容にします。

---

# 15. 表・営業時間・診療時間

表は見た目だけでなく、情報の読み取りやすさを重視します。

```html
<table>
  <thead>
    <tr>
      <th>時間帯</th>
      <th>月</th>
      <th>火</th>
    </tr>
  </thead>
  <tbody>
    ...
  </tbody>
</table>
```

## 注意

- 休業日・休診日の記号を統一する。
- 同じ表が複数箇所にある場合は、内容を同期する。
- 表の外枠・角丸・背景色はデザインカンプに合わせる。

---

# 16. レスポンシブ対応

案件でレスポンシブ対応が必要な場合は、PC版を単純に縮小せず、画面幅に合わせて再配置します。

## 基本方針

- 横スクロールを発生させない。
- 文字を小さくして無理に収めない。
- 複数カラムは必要に応じて1カラム化する。
- 表は視認性を保つ。
- ナビゲーションはスマホ向けに最適化する。
- 画像の主役が見切れないよう調整する。

## ブレイクポイント

案件指定がない場合の一例：

```css
@media (max-width: 1024px) {
  /* tablet */
}

@media (max-width: 767px) {
  /* smartphone */
}
```

※ブレイクポイントはデザインに応じて決定します。

---

# 17. アクセシビリティ

最低限、以下を意識します。

- 画像に適切な `alt`
- 見出し構造を正しくする
- リンク先が分かる文言にする
- 文字と背景のコントラストを確保する
- 小さすぎる文字を避ける
- 色だけで状態を示さない
- キーボード操作時にフォーカス位置が分かるようにする

---

# 18. JavaScript

JavaScriptは必要な場合のみ使用します。

案件で「HTML / CSSのみ」と指定されている場合は使用しません。

### JSを使用しない例

- デザイン再現だけで済む装飾
- hover表現
- レイアウト変更
- CSSだけで対応できるUI

---

# 19. 外部ライブラリ・フレームワーク

案件指定がない限り、不要なライブラリを増やしません。

### 使用前に確認するもの

- Bootstrap
- Tailwind CSS
- jQuery
- React / Vue
- スライダーライブラリ
- アイコンライブラリ
- 外部CDN

「HTML / CSSのみ」の案件では使用しません。

---

# 20. 修正対応ルール

## 20-1. 指定箇所以外を変更しない

修正依頼が、

> 「スタッフ画像だけ変更してください」

であれば、原則としてスタッフ画像に関係する箇所以外を変更しません。

---

## 20-2. 修正の影響範囲を確認する

共通CSSを変更する場合は、

- 他セクション
- 他ボタン
- 他画像
- SP表示

へ影響しないか確認します。

---

## 20-3. 最新版をベースにする

古いHTMLへ修正を加えて、過去の変更を巻き戻さないようにします。

ファイル名にバージョンを付ける場合の例：

```text
index_v01.html
index_v02.html
index_v03.html
```

最終納品時は原則として、

```text
index.html
```

へ統一します。

---

# 21. デザイン再現時の確認ポイント

デザイン画像と実装を比較する場合は、以下を順に確認します。

1. 全体幅
2. ヘッダー高さ
3. ファーストビュー
4. セクション開始位置
5. 左右余白
6. 見出し位置
7. 文字サイズ
8. 行間
9. 写真サイズ
10. 写真トリミング
11. ボタンサイズ
12. セクション間余白
13. 背景色
14. 線・角丸・影
15. フッター

---

# 22. ローカル確認

納品前に、必ず以下を確認します。

## HTML

`index.html` を直接ブラウザで開く。

### 確認項目

- CSSが読み込まれている
- 画像が表示される
- フォントが極端に崩れていない
- 相対パスが正しい
- 外部環境に依存していない
- レイアウトが崩れていない

---

# 23. ブラウザ確認

最低限、以下で表示確認を行います。

- Google Chrome
- Microsoft Edge

必要に応じて、

- Safari
- Firefox

も確認します。

---

# 24. 納品前チェックリスト

## ファイル

- [ ] `index.html` がある
- [ ] `css/style.css` がある
- [ ] `assets/` がある
- [ ] 不要ファイルがない
- [ ] ファイル名が文字化けしていない

## HTML / CSS

- [ ] CSSが正しく読み込まれる
- [ ] HTMLを直接開いても表示される
- [ ] 画像がすべて表示される
- [ ] 絶対パスが残っていない
- [ ] HTMLの文字コードがUTF-8
- [ ] 見出し構造が不自然ではない

## デザイン

- [ ] 配色が合っている
- [ ] 左右余白が合っている
- [ ] セクション間余白が合っている
- [ ] 文字サイズが合っている
- [ ] 行間が合っている
- [ ] 写真のトリミングが合っている
- [ ] ボタン位置が合っている
- [ ] 影・線・角丸が合っている

## 原稿

- [ ] 勝手な文章追加がない
- [ ] 確定原稿と一致している
- [ ] 電話番号が統一されている
- [ ] 住所が統一されている
- [ ] 営業時間・診療時間が統一されている

---

# 25. 納品ルール

最終納品時は、必要なファイル一式を1つのフォルダにまとめます。

```text
project/
├─ index.html
├─ css/
│  └─ style.css
└─ assets/
```

ZIP化する前に、**ZIP化する予定のフォルダ内にある `index.html` を直接開いて表示確認する**ことを必須とします。

---

# 26. AI・コード生成ツールへ依頼する際のルール

AIにサイト実装や修正を依頼する場合も、本ルールを適用します。

## 指示テンプレート

```text
あなたはプロのフロントエンドエンジニアです。

添付の完成デザインを忠実に再現してください。

【重要ルール】
- 指定されていない文章を追加・変更しない
- 掲載原稿がある場合は、その文言をそのまま使用する
- 支給画像以外を勝手に使用しない
- 画像生成は依頼した場合のみ行う
- HTML / CSSの相対パスを使用する
- 指定箇所以外を不用意に変更しない
- 最新の修正指示を優先する
- ローカルでindex.htmlを直接開いてもCSS・画像が表示される構成にする

【ファイル構成】
index.html
css/style.css
assets/

【納品】
上記一式をZIPにまとめる
```

---

# 27. 案件固有ルール：佐藤医院サイト

このセクションは、現在の佐藤医院Webサイト制作で使用する場合の追加ルールです。

## 基本カラー

```css
:root {
  --color-text: #171717;
  --color-primary: #0f924a;
  --color-bg: #f4f9fa;
  --color-white: #ffffff;
}
```

## フォント

```text
Noto Sans JP
Regular 400
Medium 500
Bold 700
```

## 基本仕様

- HTML / CSSのみ
- PCデザインを優先して忠実に再現
- セマンティックHTMLを使用
- 文字コード UTF-8
- 画像は `./assets/` 以下から参照
- ロゴ・画像は原則として支給素材を使用
- 掲載原稿は原則として改変しない

## コンテンツ上の注意

- 基本情報マスタの内容を正として使用する
- 診療時間はページ内の複数箇所で矛盾させない
- お知らせは指定された文言を使用する
- 人物写真の顔・頭頂部・身体が不自然に切れないよう調整する
- 高齢者にも読みやすい文字サイズを意識する
- 清潔感・親しみやすさ・地域に根ざした印象を保つ
- 必要以上に派手なビジュアルにしない

---

# 28. 更新履歴

| 日付 | バージョン | 内容 |
|---|---:|---|
| 2026-09-10 | 1.0 | 初版作成 |

---

## 運用メモ

このルールブックは固定ではなく、制作中に同じ修正が何度も発生した場合、その内容を新しいルールとして追記します。

特に以下は、案件を重ねるごとに更新します。

- CSS命名ルール
- コンテンツ幅
- 余白ルール
- レスポンシブの基準
- 画像トリミングルール
- 納品チェック
- よく起きるミスと防止策
